apiVersion: v1
kind: ConfigMap
metadata:
  name: worker-config-map
  namespace: budget
data:
  appsettings.Production.json: |
    {
      "ConnectionStrings": {
        "budgetdb": "{{dbConnectionString}}",
        "nats": "{{natsConnectionString}}"
      }
    }
